apiVersion: v1
kind: Service
metadata:
  name: httpd-service
  namespace: default
spec:
  type: NodePort # This will provision a Linode NodeBalancer automatically
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: httpd
